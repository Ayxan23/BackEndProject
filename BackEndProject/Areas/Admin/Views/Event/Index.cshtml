@model List<Event>
@{
    ViewData["Title"] = "Events";
}

<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Events</h4>
                    <div class="table-responsive">
                        <a class="btn btn-primary" asp-action="Create">Create</a>
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Image</th>
                                    <th>Name</th>
                                    <th>Venue</th>
                                    <th>StartTime</th>

                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var eventItem in Model)
                                {
                                    <tr>
                                        <td>
                                            <img src="~/assets/img/event/@eventItem.Image" />
                                        </td>
                                        <td>@eventItem.Name</td>
                                        <td>@eventItem.Venue</td>
                                        <td>@eventItem.StartTime</td>
                                        <td>
                                            <a class="btn btn-success" asp-action="Detail" asp-route-id="@eventItem.Id">Detail</a>
                                            <a class="btn btn-warning" asp-action="Update" asp-route-id=@eventItem.Id>Update</a>
                                            <a class="btn btn-danger" asp-action="Delete" asp-route-id=@eventItem.Id>Delete</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
