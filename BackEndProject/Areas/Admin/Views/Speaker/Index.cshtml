@model List<Speaker>
@{
    ViewData["Title"] = "Speakers";
}

<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Speakers</h4>
                    <div class="table-responsive">
                        <a class="btn btn-primary" asp-action="Create">Create</a>
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Image</th>
                                    <th>FullName</th>

                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var speaker in Model)
                                {
                                    <tr>
                                        <td>
                                            <img src="~/assets/img/event/@speaker.Image" />
                                        </td>
                                        <td>@speaker.FullName</td>
                                        <td>
                                            <a class="btn btn-success" asp-action="Detail" asp-route-id="@speaker.Id">Detail</a>
                                            <a class="btn btn-warning" asp-action="Update" asp-route-id=@speaker.Id>Update</a>
                                            <a class="btn btn-danger" asp-action="Delete" asp-route-id=@speaker.Id>Delete</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
